# 验收确认

## 确认范围

用户确认执行以下全项目 UI 改造：

- 统一颜色、字体、间距、圆角、边框、阴影和状态语义。
- 统一 Sidebar、WorkspaceHeader、页面标题区和内容层级。
- 统一按钮、表单、卡片、表格、标签、提示、空状态和抽屉。
- 覆盖 Runs、Workflow、Terminal、Artifacts、Gates、Knowledge、Recovery、Settings 等主要模块。
- 添加克制的页面进入、导航、卡片、抽屉、加载和状态变化动画。
- 增加窄屏适配、键盘聚焦反馈和 `prefers-reduced-motion`。
- 保持 Terminal 深色专业风格，不改变业务逻辑、API 和 `.harness` 协议。

## 用户决定

- 决定：接受需求范围、方案设计、验收标准和实施计划。
- 用户原始确认：`确定`
- 确认时间：2026-07-27

## 未解决事项

无。可以进入 `DEVELOPMENT`。
