# 实施计划

1. 新增源码文案回归断言，禁止主要区域继续使用专业术语。
2. 修改 `RunsPage.tsx` 的标题、状态说明、统计项和按钮。
3. 将目标/Run 分支改为主项目/任务代码表达，提交改为“版本记录”。
4. 把 SHA、Fast-forward 等信息集中到技术详情折叠区。
5. 执行 Renderer typecheck、tests、build。

## 回滚

仅回退文案和技术详情区，不影响 Runtime 或 Git 状态。
