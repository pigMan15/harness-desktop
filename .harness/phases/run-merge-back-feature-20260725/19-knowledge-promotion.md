# Knowledge Promotion

- Merge-back for run worktrees must use Git operations, not file copying.
- Safe defaults are important: refuse dirty target worktrees, dirty run worktrees, detached HEAD, and non-fast-forward merges.
- Merge metadata should be written to the authoritative run state for auditability.

