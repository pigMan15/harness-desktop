# Implementation Plan

1. Add runtime `merge_run_back(...)` service.
2. Expose `run.mergeBack` JSON-RPC.
3. Add Electron IPC/preload bridge.
4. Add Runs page merge-back button.
5. Record merge metadata on the run state.
6. Verify with Python compilation and TypeScript checks.

