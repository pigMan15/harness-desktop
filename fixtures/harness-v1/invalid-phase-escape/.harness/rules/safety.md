# 安全规则

目的：防止不可逆或高影响操作。

## 必须先确认

- 生产部署。
- 数据删除或迁移。
- 强推。
- 凭证变更。
- 权限变更。
- 大范围递归删除。
- 运行来自互联网的不可信脚本。

## 永远不要

- 不要在阶段产物中暴露密钥。
- 不要改写用户变更，除非用户明确要求。
- 不要把失败或未执行的门禁标记为 PASS。
- 不要隐藏不确定性。

## 默认拒绝

如果不确定某个动作是否安全，停止并询问。在 `.harness/state.json` 中记录阻塞原因。

